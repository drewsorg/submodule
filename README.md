# submodule

This repository includes a submodule for testing purposes.

## Submodules

- `external/gitignore` - GitHub's gitignore templates collection

## Usage

To clone this repository with submodules:

```bash
git clone --recursive <repo-url>
```

Or if already cloned:

```bash
git submodule update --init --recursive
```